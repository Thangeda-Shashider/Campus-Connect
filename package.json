{
  "name": "campusconnect",
  "version": "1.0.0",
  "private": true,
  "description": "CampusConnect — College Event Management Platform",
  "scripts": {
    "dev": "npm run dev --prefix client",
    "build": "npm run build --prefix client",
    "preview": "npm run preview --prefix client"
  }
}
